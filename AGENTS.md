# 차팀장 AI 직원진단 — 최상위 작업 규칙

> **이 저장소에서 수행하는 모든 코드·UI·QA·배포 작업은 이 문서를 최우선으로 따른다.**
> ChatGPT, ChatGPT Web/Desktop, Codex, Claude, 기타 코딩 에이전트가 저장소를 수정할 때 동일하게 적용한다.

## 0. 작업 시작 전 필수 읽기

작업자는 수정 전에 반드시 아래 순서로 확인한다.

1. `AGENTS.md` — 최상위 작업·배포 규칙
2. `QA_CHECKLIST.md` — 배포 전 회귀 QA 기준
3. `CHANGELOG.md` — 최근 변경과 이미 해결한 문제
4. 디자인 변경이면 `.agents/skills/cha-design-director/SKILL.md`도 확인

이 문서와 다른 지시가 충돌하면 사용자의 최신 명시적 요청을 우선하되, **QA 생략·즉시 production 배포는 사용자가 명시적으로 승인하지 않는 한 금지**한다.

---

## 1. 가장 중요한 원칙

### 수정 후 바로 production 배포하지 않는다

기본 흐름은 반드시 아래 순서를 따른다.

`요청 정리 → staging 수정 → 자동 검사 → 전체 회귀 QA → 모바일 렌더링 QA → 사용자 확인 → main production 반영`

- 작은 CSS 수정도 예외가 아니다.
- 한 화면만 바꿔도 전체 핵심 흐름을 다시 확인한다.
- 사용자가 명시적으로 `바로 운영 반영`을 요청한 경우에만 production 직행을 허용한다.

### production은 깨끗한 단일 소스여야 한다

- 배포된 loader 위에 `html.replace(...)`, CSS 덧씌우기, 문자열 치환 핫픽스를 버전마다 누적하지 않는다.
- 동일 문제를 임시 patch로 세 번 이상 덮지 않는다.
- 누적 patch가 생겼다면 다음 기능 작업 전에 **최종 상태를 원본 소스에 평탄화(flatten)** 한다.
- `index.html`이 현재 운영 제품의 실제 상태를 설명할 수 있어야 한다.

### 코드 PASS와 UI PASS를 동일하게 취급하지 않는다

- 문법 검사 성공 ≠ 사용자 화면 정상.
- DOM/JS 테스트 성공 ≠ 모바일 레이아웃 정상.
- 배포 전 실제 viewport 렌더링 확인이 필수다.

---

## 2. 모바일 우선 QA 규칙

이 프로젝트의 주요 사용 환경은 모바일이므로 아래를 기본 QA 해상도로 사용한다.

- 390px 폭 — iPhone 주요 기준
- 430px 폭 — 큰 iPhone 기준
- 데스크톱 1280px 이상

모바일에서 반드시 확인한다.

- 가로 스크롤 없음
- `overflow`, `nowrap`, `min-width`, fixed width 때문에 잘리는 요소 없음
- 한글 단어가 한 글자씩 세로로 쪼개지지 않음
- 긴 업무명이 자연스럽게 줄바꿈됨
- 버튼/진행칩/점수 카드가 viewport 밖으로 나가지 않음
- Safari/인앱 브라우저 하단 chrome이 CTA나 마지막 내용을 가리지 않음
- `env(safe-area-inset-bottom)` 고려
- 마지막 콘텐츠까지 실제로 스크롤 가능
- 터치 타깃이 지나치게 작지 않음

### 모바일 문제를 발견했을 때

해당 selector 하나만 고치지 않는다.

1. 같은 패턴의 `overflow`, `white-space`, `min-width`, `grid-template-columns`, fixed width/height를 전체 소스에서 검색한다.
2. 같은 컴포넌트가 다른 단계에서도 쓰이는지 확인한다.
3. 수정 후 질문 화면 + 무료 결과 + 심층 결과 + 최하단 CTA까지 다시 렌더링한다.

---

## 3. 핵심 사용자 플로우 회귀 테스트

모든 release candidate는 최소 아래 시나리오를 통과해야 한다.

1. 첫 화면 → 무료진단 시작
2. 업무유형/업종 입력
3. 업무 다중선택
4. 각 업무 질문 전체 진행
5. 무료 결과 정상 출력
6. 무료 결과 → 심층진단 승계
7. 직접 심층진단 시작
8. 심층 질문 전체 진행
9. 심층 결과 TOP3 출력
10. 결과 산정 기준 펼치기/접기
11. `내일 할 일 복사하기`
12. `전체 결과 복사`
13. 카카오 채널 상담 CTA
14. 새로고침 후 진행상태 복구
15. 처음으로 돌아가기

한 항목이라도 FAIL이면 production 배포하지 않는다.

---

## 4. 입력·키보드·버튼 UX 규칙

### 선택 동작

- 선택 버튼 클릭은 **선택만** 한다.
- 자동으로 다음 질문으로 넘어가지 않는다.
- 다음 이동은 `다음` 버튼이 담당한다.
- 데스크톱에서는 Enter를 보조 이동 수단으로 사용할 수 있다.
- 모바일에서는 `Enter 안내`를 노출하지 않는다.

### 텍스트 입력

- Enter 이동은 Korean IME 조합 중에는 실행하지 않는다.
- `isComposing`, keyCode 229를 고려한다.

### CTA

- 동일 중요도의 CTA는 높이·패딩·타이포 토큰을 통일한다.
- 중요도 차이는 크기 난립이 아니라 Fill / Outline / Text link 계층으로 표현한다.
- 버튼처럼 보이는 요소는 실제 버튼 동작을 가져야 한다.

---

## 5. 한글 타이포그래피 규칙

기본적으로 다음을 지킨다.

- `word-break: keep-all`
- 가능한 경우 `overflow-wrap: normal`
- 핵심 제목은 `clamp()` 사용
- 좁은 폭에서 문구를 억지로 깨기 전에 container/grid/font-size를 먼저 조정
- `overflow-wrap:anywhere`는 특별한 이유가 없으면 사용하지 않음

특히 아래와 같은 업무명은 자연스럽게 유지한다.

- 고객·회원 문의 답변
- 자료 취합·보고서 초안
- 블로그·SNS 작성
- 재구매·재등록 안내

---

## 6. 결과 페이지 품질 기준

결과 페이지는 `점수표`가 아니라 **작은 업무 컨설팅 결과**처럼 느껴져야 한다.

사용자는 결과 페이지에서 최소 아래 5가지를 즉시 이해해야 한다.

1. 내 1순위 AI 직원/업무가 무엇인지
2. 왜 1순위인지
3. 실제로 어떻게 작동할지
4. 무엇을 준비해야 할지
5. 내일 무엇부터 하면 되는지

기본 정보 순서:

`결론 → WHY → HOW → 준비물/병목 → TOP3 비교 → 내일 할 일 → 상담 CTA`

- TOP3 전체를 모두 장문으로 설명하지 않는다.
- 1순위만 상세 분석한다.
- 계산식/2축 분석은 보조 정보로 접어둘 수 있다.
- 보라/플럼 등 진한 배경 위 텍스트는 충분한 명도 대비를 확보한다.
- 마지막 CTA는 브라우저 하단 UI에 가리지 않아야 한다.

---

## 7. 복사 및 외부 링크 규칙

### 복사

모바일 인앱 브라우저를 고려해 다음 순서로 처리한다.

1. Clipboard API
2. 실패 시 안전한 fallback
3. 성공/실패 시 사용자에게 명확한 피드백 표시

버튼을 눌렀는데 아무 반응이 없는 상태는 release blocker다.

### 카카오 상담 링크

현재 검증된 상담 채널:

`https://pf.kakao.com/_xgudwX/chat`

사용자가 새 링크를 명시적으로 주기 전에는 다른 카카오 링크로 임의 변경하지 않는다.

상담 CTA 인접 문구에는 다음 의미가 포함되어야 한다.

> 결과가 애매하거나 실제 적용에서 막히면, 진단 결과를 들고 차팀장 채널에 문의할 수 있다.

---

## 8. 디자인 규칙

디자인 변경 시 `.agents/skills/cha-design-director/SKILL.md`를 우선 참고한다.

브랜드 기본 방향:

- Main: ChaTeam Coral 계열
- Secondary: Deep Plum
- Background: Warm ivory / cream
- Positive: Green
- 대부분의 화면은 flat
- 선택 컨트롤, primary CTA, 핵심 KPI 정도에만 tactile depth 사용
- generic AI SaaS 느낌, card spam, 과도한 gradient/glass/shadow를 피한다.

---

## 9. 변경 작업 방식

수정 요청을 받으면 바로 코드부터 바꾸지 말고 먼저 영향 범위를 정리한다.

예:

- `모바일에서 잘림` → 해당 화면뿐 아니라 전체 responsive layout 패턴 확인
- `복사 안 됨` → 모든 copy CTA 확인
- `버튼 크기 다름` → 전체 CTA token 확인
- `카카오 링크 변경` → 코드 내 모든 해당 URL 검색

### 변경 완료 후 반드시 기록

`CHANGELOG.md`에 다음을 기록한다.

- 변경 내용
- 변경 이유
- 영향받는 화면
- 수행한 QA
- 알려진 제한사항

---

## 10. 배포 규칙

### branch 역할

- `main` = production
- `staging` = 다음 배포 후보

기본적으로 기능/UI 수정은 staging에서 진행한다.

### production 승격 조건

아래가 모두 충족되어야 한다.

- `QA_CHECKLIST.md` 필수 항목 PASS
- 문법/기능 자동 검사 PASS
- 390px, 430px 모바일 렌더링 PASS
- 데스크톱 PASS
- 결과 페이지 마지막까지 스크롤 PASS
- 복사/외부링크 PASS
- 사용자에게 staging 확인 링크를 제공했고 승인받음

### 배포 후

- production을 새로고침해서 실제 반영 확인
- cache-busting query를 사용해도 됨
- 배포 완료를 말하기 전에 GitHub의 실제 `main` 파일/commit을 다시 확인

---

## 11. 금지사항

- QA 없이 `수정했습니다 / 배포했습니다`라고 말하지 않는다.
- 실제 확인하지 않은 링크를 `검증됨`이라고 부르지 않는다.
- 모바일 화면을 보지 않고 `모바일 QA 완료`라고 말하지 않는다.
- 특정 화면 하나의 PASS를 전체 제품 PASS로 간주하지 않는다.
- 문제 해결을 위해 기존 정상 기능을 임의로 제거하지 않는다.
- 사용자 요청 없이 scoring logic을 변경하지 않는다.
- 이전에 해결된 버그를 재도입하지 않는다.

---

## 12. 작업 종료 보고 형식

작업을 마쳤을 때 최소한 다음을 보고한다.

- 무엇을 바꿨는지
- 어떤 화면에 영향이 있는지
- 어떤 QA를 실제 수행했는지
- staging인지 production인지
- 사용자가 확인해야 할 정확한 항목

`완료`라는 표현은 실제 배포/QA 상태와 일치해야 한다.
